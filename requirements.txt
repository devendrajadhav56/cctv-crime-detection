# Phase 1–2: dataset indexing and offline zero-shot inference.
# CPU PyTorch is enough on Windows for --dry-run and env checks.
# On the GB10 box (aarch64, CUDA 13.0), install torch from the official
# CUDA wheel index instead of reusing a Windows/x86 wheel. See README.
torch>=2.2.0
torchvision>=0.17.0
opencv-python>=4.9.0
PyYAML>=6.0
pandas>=2.0.0
tqdm>=4.66.0
transformers>=4.40.0
Pillow>=10.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
imageio-ffmpeg>=0.5.1
ftfy>=6.1.0
regex>=2023.0.0
scipy>=1.11.0  # VadCLIP's DistanceAdj layer uses scipy.spatial.distance
setuptools<81  # VadCLIP's vendored CLIP fork does `from pkg_resources import packaging`, removed in setuptools>=81
